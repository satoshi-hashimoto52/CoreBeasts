namespace CoreBeasts.Units
{
    /// <summary>
    /// コアビーストの属性。
    /// 属性間の勝敗判定（3すくみ）は本工程では実装しません。
    /// </summary>
    public enum UnitAttribute
    {
        Red = 0,
        Green = 1,
        Blue = 2,
    }
}
